# -*- coding: utf-8 -*-

a, b, c = 1, 2, 2

password = 'a123'

result = (a == b)   # false
result = (b == c)   # true
result = ('a234' == password)   # false

# !=  <  >  <=  >=  operators can be used in the same way